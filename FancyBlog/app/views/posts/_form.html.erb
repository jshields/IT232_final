<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <div class="blackText"><%= f.text_field :title %></div>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <div class="blackText"><%= f.text_area :body %></div>
  </div>
  <div class="field">
  <%= f.label :tag_list, "Tags (separated by commas)" %><br />
  <div class="blackText"><%= f.text_field :tag_list %></div>
  </div>
  <div class="actions blackText">
    <%= f.submit %>
  </div>
<% end %>
